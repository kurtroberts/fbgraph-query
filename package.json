{
  "name": "fbgraph-query",
  "version": "1.0.0",
  "description": "query the facebook social graph from the command line",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kurt Roberts",
  "license": "MIT",
  "dependencies": {
    "fbgraph": "^1.4.1",
    "yargs": "^8.0.1"
  }
}
